resultIndex <- c()
counter <- c(1)
for(index in 1:dataLength)
{
  #resultIndex <- which(busanWeather$일시 == indexA) + resultIndex 
  #indexA
  onlyContainsDate[counter]
  counter <- counter + 1
}

#onlyContainsDate[1][1]

#busanWeatherIndex <- which(busanWeather$일시 == onlyContainsDate)
#head(busanWeather)
#str(busanWeather)
#head(busanWeatherIndex)

#CrawlTargetAfromTargetB <- function(targetA, targetB)
#{
#  resultIndex <- c()
# for(indexOfA in targetA)
# {
#   counter <- 1
#   for(indexOfB in targetB)
#   {
#     if(!is.na(indexOfA["일시"] == indexOfB)) {
#       resultIndex <- resultIndex + counter
#     }
#     counter <- counter + 1
#   }
# }
#  return (resultIndex)
#}
#busanWeatherIndex <- CrawlTargetAfromTargetB(busanWeather, onlyContainsDate)
#head(busanWeatherIndex)

